{
  "name": "aisims-studio",
  "version": "0.0.0",
  "private": true,
  "description": "AI Sims Creator — monorepo root (pnpm workspace for the Node/TS areas).",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test:run": "pnpm -r test:run"
  }
}
