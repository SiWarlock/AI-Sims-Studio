"""Phase-0 scaffold placeholder for the eval harnesses.

Real harnesses (EVAL-001…009), metrics, judges, datasets, and bridges land with the eval
suite (these ship via the eval path, not /tdd). This placeholder only proves the area's
pytest toolchain collects + runs clean while the area is empty.
"""


def test_evals_scaffold_present() -> None:
    assert True
