"""Phase-0 scaffold placeholder for the Blender worker.

The real bpy job-contract code (contracts/gate/geom/render/cli/io per the area module
layout) and its tests land in the Blender track's first phase; this placeholder only
proves the area's pytest toolchain collects + runs clean while the area is empty.
"""


def test_blender_worker_scaffold_present() -> None:
    assert True
