// Phase-0 skeleton entry for the desktop UI (React 19 + Vite + Electron).
//
// The real surfaces (the 12 UI surfaces, IPC client + SSE subscription, Electron main/preload
// per the area module layout) land in the desktop track's first phase. This placeholder only
// gives the strict TS toolchain an input so `tsc --noEmit`, ESLint, and Vitest run clean while
// the area is empty.
export {};
