# Alembic config for the §13 store. Migrations are driven programmatically via
# store.migrations.runner.run_migrations (the connection is shared in cfg.attributes),
# so no sqlalchemy.url is set here; the runner sets an absolute script_location.
[alembic]
script_location = migrations
prepend_sys_path = .
path_separator = os
